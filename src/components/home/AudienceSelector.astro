---
import { audiences } from '../../data/home';
import HomePicture from './HomePicture.astro';
---

<section id="examples" tabindex="-1" aria-labelledby="examples-heading">
  <div class="v3-examples-head">
    <span>Where this takes shape</span>
    <h2 id="examples-heading">Different businesses. Different opportunities.</h2>
    <div>
      <p>Rushes works across industries. The right work depends on what the business is trying to accomplish. These are a few of the markets where the approach has taken shape, with the move that usually comes first. They are examples, not a limit on who Rushes can help.</p>
    </div>
  </div>
  <div class="v3-range">
    {audiences.map((audience, index) => (
      <article
        class:list={['v3-range-card', index === 0 && 'lead']}
        data-visual-truth={audience.visual.truthClass}
      >
        <figure>
          <HomePicture
            asset={audience.visual}
            sizes={index === 0 ? '(max-width: 760px) calc(100vw - 40px), 48vw' : '(max-width: 760px) calc(100vw - 40px), 24vw'}
          />
        </figure>
        <span class="who-card-route">{audience.routeLabel}</span>
        <h3>{audience.label}</h3>
        <p>{audience.summary}</p>
        <p class="v3-range-move"><b>Usual first move</b>{audience.firstMove}</p>
      </article>
    ))}
  </div>
</section>
