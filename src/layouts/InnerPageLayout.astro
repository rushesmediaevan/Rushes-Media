---
import type { CapabilityPage } from '../data/capability-pages';
import CapabilityLayout from './CapabilityLayout.astro';

interface Props { page: CapabilityPage; }
const { page } = Astro.props;
---

<CapabilityLayout page={page} />
